<?xml version="1.0" encoding="utf-8"?>
<manifest
  package="com.osovskiy.bmwinterface"
  xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
  <uses-permission android:name="android.permission.USB_PERMISSION"/>

  <uses-feature android:name="android.hardware.usb.host"/>

  <permission
    android:name="com.osovskiy.bmwinterface.permission.RECEIVE_MSG_PERMISSION"
    android:label="@string/label_permission_receive"
    android:description="@string/desc_permission_receive"
    android:protectionLevel="normal"/>

  <permission
    android:name="com.osovskiy.bmwinterface.permission.SEND_MSG_PERMISSION"
    android:label="@string/label_permission_send"
    android:description="@string/desc_permission_send"
    android:protectionLevel="normal"/>

  <application
    android:allowBackup="true"
    android:icon="@drawable/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/AppTheme">

    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:theme="@style/AppTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>

      <meta-data
        android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
        android:resource="@xml/device_filter"/>
    </activity>

    <service
      android:name=".BMWiService"
      android:exported="false"
      android:label="BMWinterfaceService"
      android:process=":bmwinterface"/>

    <receiver
      android:name=".BMWiReceiver"
      android:permission="com.osovskiy.bmwinterface.permission.SEND_MSG_PERMISSION">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED"/>
        <action android:name="com.osovskiy.bmwinterface.ACTION_SEND_BUS_MESSAGE"/>
      </intent-filter>
    </receiver>

    <receiver
      android:name=".ActuationsWidgetProvider"
      android:icon="@drawable/ic_launcher"
      android:label="BMWinterface Actuations">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
      </intent-filter>

      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/actuations_widget_info"/>
    </receiver>

    <receiver
      android:name=".StatusWidgetProvider"
      android:icon="@drawable/ic_launcher"
      android:label="BMWinterface Service">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
      </intent-filter>

      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/status_widget_info"/>
    </receiver>

  </application>

</manifest>
